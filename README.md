# Steps need to flow to setup the keycloak on local server is given in document in the folder
* user_login.html file need to copy inside the nginx server -> /var/www/html
* The setup is for linux system
